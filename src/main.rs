//tratandose de nombres de archivo, se prefiere usar underscore a camel en Rust
//para compilar se usa rustc [filename.rs]
//para ejecutar ./filename(compilado)

fn main(){ //funcion especial y reservada main, marca el inicio del programa
    println!("Hello world Rust!"); //macro que imprime mensaje (motivo del !)
}